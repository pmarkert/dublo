node_modules/
dist/
resources/skills/
.env
.DS_Store
.dublo
reports/

# dist/ and resources/skills/ are build artifacts, produced by `npm run build`
# and regenerated by the `prepare` hook on install (see scripts/prepare.mjs).
